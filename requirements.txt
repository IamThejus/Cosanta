# Core Cosanta dependencies (Android / Termux, aarch64, Python 3.11+)
#
# Install order matters on Termux — see README.md for the system packages
# (portaudio, etc.) you must install with `pkg` first.

numpy>=1.24
sounddevice>=0.4.6      # PortAudio microphone capture + playback
pvporcupine>=3.0        # wake-word detection
faster-whisper>=1.0     # speech-to-text (pulls in ctranslate2)
groq>=0.11              # Groq LLM SDK
piper-tts>=1.2          # text-to-speech

# Optional: nicer .env loading (a minimal fallback parser is built in).
python-dotenv>=1.0
